apiVersion: apps/v1
kind: Deployment
metadata:
  name: cage-redis
spec:
  replicas: 1
  selector:
    matchLabels:
      app: cage-redis
  template:
    metadata:
      labels:
        app: cage-redis
    spec:
      containers:
        - name: redis
          image: redis:7-alpine
          ports:
            - containerPort: 6379
---
apiVersion: v1
kind: Service
metadata:
  name: cage-redis
spec:
  selector:
    app: cage-redis
  ports:
    - port: 6379
      targetPort: 6379
      protocol: TCP
      name: redis
